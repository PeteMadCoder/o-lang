import "std/string.olang";

fn printf(fmt: *char, val: int);

fn main() -> int {
    var msg = new String("Hello, World!");
    
    // Check length (should be 13)
    if (msg.length != 13) {
        return 1;
    }
    
    return 0;
}
