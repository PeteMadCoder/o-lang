struct SimpleString {
    buffer: *byte;
    length: int;

    new(len: int) {
        this.length = len;
        this.buffer = 0;  // Just set to null for testing
    }
}

fn main() -> int {
    var s = new SimpleString(10);
    return 0;
}