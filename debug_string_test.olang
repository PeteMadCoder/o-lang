import "std/string.olang";
import "std/io.olang";

fn main() -> int {
    println("Starting string debug test...");

    // Test basic string creation
    var msg = new String("Hello, World!");
    println("String created successfully");

    // Test creating an empty string
    var empty = new String(10);
    println("Empty string created successfully");

    // Test appending
    var str1 = new String("Hello");
    var str2 = new String(" World");
    str1.append(&str2);
    println("String append completed");

    // Test string concatenation with operator
    var str3 = new String("Test");
    var str4 = new String("Concat");
    // Note: This might not work depending on how op_add is implemented
    // var result = str3.op_add(&str4);  // Direct call to test

    println("All tests completed");
    return 0;
}