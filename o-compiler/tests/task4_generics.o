struct Box<T> {
    T value;
    
    new(T v) {
        this.value = v;
    }
    
    fn get() -> T {
        return this.value;
    }
}

fn main() -> int {
    var b = new Box<int>(42);
    return b.get();
}
