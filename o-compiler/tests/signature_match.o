struct TestStruct {
    int data;
    
    fn process(int input) -> int {
        return input * 2;
    }
}

// External declaration matching the expected mangled signature
// This should match: TestStruct_process(*TestStruct this, int input) -> int
fn TestStruct_process(*TestStruct this, int input) -> int;

fn main() -> int {
    // If the signatures match, this should compile without errors
    return 0;
}
