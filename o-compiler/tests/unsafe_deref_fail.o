fn unsafeDereference() -> int {
    var x = 42;
    var ptr = &x;
    
    // This should cause a safety error - dereference in safe context
    var value = *ptr;
    
    return value;
}

fn main() -> int {
    return unsafeDereference();
}
