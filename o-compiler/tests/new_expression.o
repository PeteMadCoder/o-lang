struct Point {
    int x, y;
    
    new(int x, int y) {
        // Constructor - will be called by 'new Point(10, 20)'
        var temp = x + y; // Simple constructor body
    }
    
    fn getValue() -> int {
        return 42;
    }
}

fn testNewExpression() -> *Point {
    // This should allocate memory and call Point_new constructor
    var point = new Point(10, 20);
    return point;
}

fn main() -> int {
    var p = testNewExpression();
    return 0;
}
