fn safeAddressOperations() -> int {
    // These are all safe operations - no unsafe block needed
    var x = 10;
    var y = 20;
    var z = 30;
    
    var ptr_x = &x;
    var ptr_y = &y;
    var ptr_z = &z;
    
    var ptr_ptr_x = &ptr_x;
    
    return 100;
}

fn main() -> int {
    var result = safeAddressOperations();
    return result;
}
