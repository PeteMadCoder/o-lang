struct Point {
    int x, y;
    
    new(int x, int y) {
        var sum = x + y;
        return;
    }
}

fn main() -> int {
    // Call constructor as a regular function for now
    var result = Point_new(10, 20);
    return 0;
}
