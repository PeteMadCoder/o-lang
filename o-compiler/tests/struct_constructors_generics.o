struct Point {
    int x, y;
    
    new(int x, int y) {
        // Constructor body - for now just empty
        var temp = x + y;
    }
    
    fn getValue() -> int {
        return 42;
    }
}

struct Container<T> {
    T value;
    
    new(T val) {
        var temp = 0;
    }
}

fn main() -> int {
    return 0;
}
