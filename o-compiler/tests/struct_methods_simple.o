struct Point {
    int x, y, z;
    float a, b;
    
    fn getConstant() -> int {
        return 42;
    }
}

struct Rectangle {
    int width, height;
    
    fn getArea() -> int {
        return 10 * 20;  // hardcoded for now
    }
}

fn main() -> int {
    return 0;
}
