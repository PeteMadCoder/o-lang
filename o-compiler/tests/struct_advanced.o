struct Point {
    int x, y, z;
    float a, b;
}

struct Rectangle {
    int width, height;
    
    fn area() -> int {
        return width * height;
    }
    
    fn perimeter() -> int {
        return 2 * (width + height);
    }
}

fn main() -> int {
    return 0;
}
