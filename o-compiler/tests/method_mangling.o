struct Point {
    int x, y;
    
    fn magnitude() -> int {
        // This method should be mangled to Point_magnitude
        // and should receive 'this' as first parameter
        return 42; // Simplified for now
    }
    
    fn distance(int other_x, int other_y) -> int {
        // This should be mangled to Point_distance
        // and receive 'this' as first parameter
        return 100; // Simplified
    }
}

fn main() -> int {
    // For now, we'll call the mangled methods directly
    // Later we'll implement proper method call syntax
    return 0;
}
