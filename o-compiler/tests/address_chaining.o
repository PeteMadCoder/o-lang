fn testAddressOfChaining() -> int {
    var x = 123;
    var ptr = &x;
    var ptrptr = &ptr;
    var ptrptrptr = &ptrptr;
    
    // This should compile successfully - we're just getting addresses
    return 42;
}

fn main() -> int {
    var result = testAddressOfChaining();
    return result;
}
