fn main() -> int {
    // Test different string types
    var empty = "";
    var simple = "Hello";
    var escaped = "Line1\nLine2\tTabbed";
    var quoted = "Say \"Hello\"";
    
    // String literals create global constant arrays
    // and return i8* pointers to first character
    
    return 0;
}
