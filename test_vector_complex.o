struct Vector<T> {
    T[16] data;
    int count;
    
    new() {
        this.count = 0;
    }
    
    fn push(T val) {
        this.data[this.count] = val;
        this.count = this.count + 1;
    }
}

fn main() -> int {
    var vec = new Vector<int>();
    vec.push(42);
    return 0;
}
