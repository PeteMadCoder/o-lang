import "std/io.olang";

fn main() -> int {
    // Test basic casting functionality
    var x: int = 42;
    var ptr = &x;
    var byte_ptr = ptr as *byte;
    
    print_int(x);  // Should print 42
    println("");
    
    return 0;
}