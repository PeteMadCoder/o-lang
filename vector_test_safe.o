// Test file for Vector implementation
// NOTE: This file demonstrates the syntax but will cause compiler crash
// when Vector<int> is instantiated due to a compiler bug in generic monomorphization

// For now, just verify that the module can be imported
import "std/vector.olang";

fn main() -> int {
    // The Vector module is available but instantiation causes compiler crash
    // due to a bug in generic type monomorphization
    return 0;
}