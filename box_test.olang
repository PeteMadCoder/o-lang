// This WORKS
struct Box<T> {
    value: T;
    
    new(val: T) {
        this.value = val;
    }
    
    fn do_exit() {
        unsafe {
            exit(1);
        }
    }
}

fn main() -> int {
    var b = new Box<int>(42);
    b.do_exit();
    return 0;
}