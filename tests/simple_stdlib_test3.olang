import "std/io.olang";
import "std/math.olang";
import "std/vector.olang";

fn add(a: int, b: int) -> int {
    return a + b;
}

fn main() -> int {
    // Test basic variable declarations with new syntax
    var x: int = 10;
    var y: int = 20;
    var sum: int = add(x, y);
    
    // Test print functions
    print_int(sum);
    
    // Test math functions
    var abs_val: int = abs(-5);
    print_int(abs_val);
    
    var min_val: int = min(10, 5);
    print_int(min_val);
    
    // Test vector operations (without calling constructor explicitly)
    var vec = new Vector<int>();  // This should call the default constructor
    
    vec.push(100);
    vec.push(200);
    vec.push(300);
    
    var vec_size: int = vec.size();
    print_int(vec_size);
    
    var first_elem: int = vec[0];
    print_int(first_elem);
    
    return sum;
}