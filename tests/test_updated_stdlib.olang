import "std/io.olang";
import "std/math.olang";
import "std/vector.olang";

fn main() -> int {
    var x = 10;
    var y = 20;
    var sum = add(x, y);  // Assuming add function exists or we define it
    
    print_int(sum);
    
    var vec = new Vector<int>();
    vec.push(5);
    vec.push(10);
    
    var first = vec[0];
    var second = vec[1];
    
    return sum;
}

fn add(x: int, y: int) -> int {
    return x + y;
}