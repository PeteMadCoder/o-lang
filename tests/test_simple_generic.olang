// Simple test without imports to see if basic generic instantiation works
struct SimpleVec<T> {
    T[4] data;
    int count;

    new() {
        this.count = 0;
    }

    fn push(T val) {
        if (this.count < 4) {
            this.data[this.count] = val;
            this.count = this.count + 1;
        }
    }
}

fn main() -> int {
    var vec = new SimpleVec<int>();
    vec.push(42);
    return vec.count;
}