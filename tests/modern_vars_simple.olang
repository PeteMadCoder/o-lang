fn main() -> int {
    // 1. Inference & Mutability
    var x = 10;
    x = 20;     // OK

    // 2. Explicit Type
    var y: int = 100;
    
    // 3. Constants
    let z = 50;
    // z = 51;  // <-- This should trigger a Compiler Error
    
    return x + z;
}