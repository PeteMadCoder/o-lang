struct Debug<T> {
    T[5] values;
    int count;
    
    fn test(T val) {
        this.values[this.count] = val;
    }
}

fn main() -> int {
    var d = new Debug<int>();
    d.test(42);
    return 0;
}
