// Point definition for import testing
struct Point {
    x: int;
    y: int;

    new(x: int, y: int) {
        this.x = x;
        this.y = y;
    }

    fn getX() -> int {
        return this.x;
    }
    
    fn getY() -> int {
        return this.y;
    }
}