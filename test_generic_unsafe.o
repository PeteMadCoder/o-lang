struct VectorWithUnsafe<T> {
    T[5] data;
    int count;

    new() {
        this.count = 0;
    }

    fn test_unsafe() {
        unsafe {
            var x = 5;
        }
    }
}

fn main() -> int {
    var vec = new VectorWithUnsafe<int>();
    return 0;
}