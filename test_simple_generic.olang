struct SimpleBox<T> {
    value: T;

    new(val: T) {
        this.value = val;
    }

    fn get() -> T {
        return this.value;
    }
}

fn main() -> int {
    var box = new SimpleBox<int>(42);
    return box.get();
}